# Task 3

1. Create a Ubuntu 24.04 based virtual machine using QEMU virtual machine monitor. Use the information in the following repository. https://github.com/gnudeep/qemu-vm-demo 

2. Connect to the virtual machine and connect to the virtual machine via VNC protocol 

3. Connect the virtual machine using SSH protocol. 
